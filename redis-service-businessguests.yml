apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    name: redis-service
    app: guestbook-app
    tier: business
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    name: redis-pod
    app: guestbook-app
    tier: business
